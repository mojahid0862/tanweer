# Tanweerul Haque Portfolio

Modern Next.js portfolio generated from `Resume TH.pdf`.

## Run

```powershell
pnpm install
pnpm dev
```

## Build

```powershell
pnpm build
```

Set `NEXT_PUBLIC_SITE_URL` to the production domain before deployment so canonical URLs, sitemap, robots, and structured data point to the live site.
